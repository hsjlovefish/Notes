`练习2.3` 读程序写结果
```cpp
    unsigned u = 10, u2 = 42;
    cout << u2 - u << endl; // 32
    cout << u - u2 << endl;  // -32-->4,294,967,264

    int i = 10, i2 = 42;
    cout << i2 - i << endl; // 32
    cout << i - i2 << endl; // -32
    // 如果表达式里既有带符号类型又有无符号类型
    // 当带符号类型取值为负时会出现异常结果, 因为带符号数会自动转换为无符号数
    // 带符号类型取值不为负不会?
    cout << i - u << endl; // 0
    cout << u - i << endl; // 0

```